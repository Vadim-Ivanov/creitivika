x=['F','u','n','c']
l=len(x)
def func(x,l):
    if len(x) == 0:
        return "non"
    y=''
    for i in range(l):
        y=y+x[i]
    return y
def setup():
    global x
    size(600,400)
    textSize(25)
    noStroke()
def draw():
    global x
    fill(0)
    text(func(x,l),300,200)
