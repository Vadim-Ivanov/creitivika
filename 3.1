i=600
j=400
f=0
f2=0
def setup():
    global i,j
    size(i,j)
def draw():
    global i,j,f,f2
    for y in range(0,320,40):
        if f > 1:
            f2=0
        if f2 == 1:
            f=0
        if f2 == 0:
            f=1
        for x in range(0,320,40):
            if f > 1:
                f=0
            if f == 1:
                fill(0)
            if f == 0:
                fill(255)
            rect(x,y,40,40)
            f+=1
        f2+=1
